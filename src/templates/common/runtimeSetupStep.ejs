<%-
  (props.packageManager === 'bun') ? `
      - name: 🥟 Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: '${props.bunVersionFile}'` : 
`
      - name: 🌿 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: '${props.nodeVersionFile}'
          cache: '${props.packageManager}'`
%>
