---
sidebar_position: 1
---

# ESLint

Set up workflow to run ESLint check on your code for every Pull Request.

## Usage

```bash
npx setup-ci --preset --lint
```

## Detailed behavior

Below you can find detailed information about what the script does with your project when generating ESLint workflow.

### Installed dependencies

- a
- b
- c
- d

### Modified files

| File                         | Changes                                                  |
| ---------------------------- | -------------------------------------------------------- |
| `.github/workflows/lint.yml` | created file with the workflow                           |
| `package.json`               | added missing dependencies, added new script "lint"      |
| `.eslintrc.json`             | created if no existing ESLint configuration was detected |

### Workflow details

:::danger
TODO
:::
